spring:
  application:
    name: shorturl
  datasource:
    url:
      jdbc:postgresql://localhost:5432/short
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update